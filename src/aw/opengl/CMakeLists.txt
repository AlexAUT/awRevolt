#OPENGL only when AW_USE_OPENGL is defined
if(AW_USE_OPENGL)
  set(IMPL_SOURCE_FILES
    ${CMAKE_CURRENT_SOURCE_DIR}/impl/gl.cpp
  )
endif()
set(SOURCE_FILES_GRAPHICS ${SOURCE_FILES_GRAPHICS} ${IMPL_SOURCE_FILES}
    ${CMAKE_CURRENT_SOURCE_DIR}/opengl.cpp
    PARENT_SCOPE)

